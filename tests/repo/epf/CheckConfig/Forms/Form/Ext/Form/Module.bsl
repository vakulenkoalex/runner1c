
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	Если Не ПустаяСтрока(ПараметрЗапуска) Тогда
		
		ЗаписьФайла = Новый ЗаписьТекста(ПараметрЗапуска);
		ЗаписьФайла.ЗаписатьСтроку(?(КлиентскоеПриложение.ПолучитьЗаголовок() = "Test config", 0, 1));
		ЗаписьФайла.Закрыть();
		
		ЗавершитьРаботуСистемы(Ложь);
		
	КонецЕсли;
	
КонецПроцедуры
